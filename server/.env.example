PORT=3000
MONGO_URI=mongodb://mongo:27017/postflow

# Optional — email alerts. Leave unset to have notifications log instead
# of send (no real mail account needed for dev or for the test suite).
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
ALERTS_FROM=postflow@localhost
